import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()

            Text("Moustache 🔥")
                .foregroundColor(.white)
                .font(.largeTitle)
        }
    }
}
